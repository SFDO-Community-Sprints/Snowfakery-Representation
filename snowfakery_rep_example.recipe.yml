# before you execute this you might need to set your
# PYTHONPATH with a command like this:
#
# $ export PYTHONPATH=.
# or
# > set PYTHONPATH=.

- plugin: faker_representation.Provider

- object: Contact
  fields:
    FirstName:
      fake: nonbinary_name
    LastName:
      fake: last_name

- object: Contact
  fields:
    FirstName:
      fake: chicago_name
    LastName:
      fake: last_name